{
  "name": "pop-party",
  "version": "1.0.0",
  "private": true,
  "description": "🎈 Pop Party — Discord Activity balloon popping game",
  "scripts": {
    "deploy": "npx wrangler pages deploy . --project-name pop-party"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
